import React from 'react';
import { PropagateLoader } from 'react-spinners';

const Spinner = () => {
  return (
    <>
      <PropagateLoader color={'black'} size={150} />
    </>
  );
};

export default Spinner;
